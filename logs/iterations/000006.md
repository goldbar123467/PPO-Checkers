# Iteration 000006 — R7 Notation and Full-State Serialization

- Work label: Phase 2 correctness baseline.
- Starting Git SHA: `f0fef0f3bd91`.
- Falsifiable objective: strict ACF move text and a versioned serialization must round-trip every
  §5.1 state field, including a forced mid-capture state, with no permissive alternate spellings.

## RED

- Focused collection failed because `checkers.rules.notation` did not exist; exit 2. Evidence:
  `logs/test-output/000006-red.txt`.
- The first full gate passed overall but showed only 78% coverage on notation validation branches.
  Evidence: `logs/test-output/000006-check.txt`.

## GREEN

- Added immutable `MovePath`, strict `11-15` / `22x18x9` parsing and formatting, and rejection of
  mixed separators, whitespace, leading zeroes, and out-of-range squares.
- Added canonical `CHK1` text with fixed uint32 hex, explicit side, capture flag, moving/origin
  squares, pending mask, both counters, and ply. Initial and forced mid-sequence states serialize
  and restore exactly.
- Added type/shape/semantic failure tests rather than leaving defensive branches unexercised.
- Increment gate: 114 tests passed; every rules module at 100% statement/branch coverage. Evidence:
  `logs/test-output/000006-check-2.txt`.

## Result

R7.1–R7.3 are implemented and locally green; published transcript replay remains external Phase 2
correctness evidence still to run.
