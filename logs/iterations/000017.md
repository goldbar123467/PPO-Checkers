# Iteration 000017 — Source-audit the statistical claim boundary

## Orientation

The corrected `e386b31` run reproduced all 4,704 earlier game records exactly. A real resume loaded
all six checkpoints and regenerated byte-identical raw and report files. Before conclusion, the
statistical source language was audited against primary publications.

## Source findings

- NIST/SEMATECH gives the two-sided uncorrected normal-approximation formula used here and also
  separately recommends a continuity correction. The experiment uses the former only.
- Wilson's original interval is for a binomial proportion; fractional draw scoring is explicitly
  a Wilson-style approximation.
- Bradley–Terry supplies a paired-comparison model, not proof that this population is transitive.
  The weighted league projection, CI, 100-Elo residual threshold, and cycle diagnostics remain
  project methods with stated assumptions.
- WCDF supports the standard rules, alternating colours, and Red moving first. All evaluated games
  nevertheless use only the standard opening, not an opening-ballot distribution.

## RED / GREEN

The new permanent test required five disclosures and failed against the prior report text:
`logs/test-output/000064-phase5-statistical-caveats-red.txt`.

The machine report now says seed independence is modeled rather than proved, names the omitted
continuity correction, identifies the single-opening scope, labels the 100-Elo threshold as
project-defined, and discloses tactical-suite selection. `make check` exited 0 with 608 tests plus
eight property tests and 100% Checkers statement/branch coverage:
`logs/test-output/000065-phase5-statistical-caveats-check.txt`.

## Next

Commit the source-audited report contract, produce final artifacts from that clean SHA, exercise
resume once more, and write the human-readable sourced Phase 5 conclusion.
