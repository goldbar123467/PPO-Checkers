# Iteration 000013 — Phase 3 Terminal Conditions and Hashing

- Work label: Phase 3 terminal/hash baseline.
- Starting Git SHA: `1d0e79342267`.
- Falsifiable objective: implement every R6 boundary and two distinct frozen keys, with incremental
  updates equal to recomputation after reachable steps and reverse updates.

## Specification Defect

Section 5.3 claims `state_key` covers everything that affects transitions, but explicitly omits
`no_progress` and `ply`; R6.3/R6.5 make both terminal-transition inputs. It also omits propagated
`sequence_origin`. BLOCK-005 records the deterministic collisions. The implementation uses the
complete source-correct key while leaving the read-only goal unchanged.

## RED

Tests were written first. Collection failed for the absent `terminal` and `zobrist` modules, then
the frozen-key characterization failed on zero placeholders and exposed the actual deterministic
keys. Evidence: `logs/test-output/000017-phase3-terminal-hash-red.txt` and
`000017-phase3-terminal-hash-green-1.txt`.

## GREEN

- R6.1/R6.2 losses, both-player 39/40 no-progress boundary, optional repetition only at move
  boundaries, 511/512 ply transition, no-agreement API, and loss-first precedence pass.
- `state_key` distinguishes capture state, sequence origin, counters, and ply; `position_key`
  ignores counters/ply and raises mid-sequence. Known initial keys freeze schema/seed behavior.
- XOR incremental changes equal full recomputation and reverse exactly through deterministic and
  50,000-step reachable trajectories.
- Focused terminal/hash modules reach 100% statement/branch coverage.
- Consolidated `make check`: exit 0; 188 tests plus eight property tests, strict Ruff/mypy, and
  every rules module at 100% statement/branch coverage. Evidence `logs/gates/phase-3.txt`.

## Result

All feasible Gate 3 work is proven. Formal Phase 3 status is BLOCKED by BLOCK-001 and BLOCK-005;
work advances to the source-correct portion of Phase 4 under the goal's escape hatch.
