# Iteration 000020 — Preserve full rollout chronology

## Objective

Implement T8 so opponent transitions remain in GAE chronology, optimization views never receive
frozen-policy actions, and a rollout slice ending mid-capture bootstraps in the correct actor frame.

## RED / correction / GREEN

The initial test failed collection because `buffer.py` did not exist (`000070-*`). The first
implementation run then exposed a hand-oracle error: an environment lane's middle positive sign
must preserve its terminal negative advantage. It also exposed a fixture that marked a frozen
policy ID trainable. Both test data defects were corrected explicitly; the implementation formula
was not changed to fit the mistaken expectation (`000071-*`).

The behavioral suite then passed. Its quality audit found missing strict-type suppressions,
formatting, and seven untested defensive branches (`000073-*`). The corrected gate passed 50 tests,
strict mypy and Ruff, with 211/211 statements and 96/96 branches covered (`000074-*`).

## Result

- complete `(time, env)` GAE precedes trainable filtering;
- policy source indices are auditable and exclude every frozen action;
- value loss defaults to the same trainable indices and exposes opponent states only explicitly;
- observations and stored masks are cloned and detached from mutable environment output;
- stable lane order makes per-environment adjacency structural;
- a final multi-jump continuation uses `sigma=+1` with the per-lane bootstrap;
- finalization is one-use, preventing accidental replay of an on-policy rollout.

## Next

Implement the exact six-block GroupNorm policy/value network from structural and numerical RED
tests, including the deferred N7 aliasing-logit regression.
