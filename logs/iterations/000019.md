# Iteration 000019 — Freeze Phase 6 oracles and implement signed GAE

## Objective

Translate the complete Gate 6 contract into independent tests, then implement only the shared
masked distribution and perspective-aware GAE foundation.

## Source audit

The original PPO and GAE papers establish the clipped surrogate and conventional GAE recursion.
Current official PyTorch documentation establishes categorical, deterministic-algorithm, and
gradient-clipping API behavior. None proves the project's two-player sign extension, which remains
project-derived and must earn confidence through independent numerical tests.

`docs/PHASE6_TEST_MATRIX.md` freezes the T1–T8, N2–N7, D2, and D3 evidence plan and labels every
item GREEN, partially green, or pending.

## RED / GREEN

The first focused invocation failed collection because `masked_categorical.py` and `gae.py` did not
exist (`logs/test-output/000066-phase6-mask-gae-red.txt`). After implementation, 28 behavioral
tests passed. A quality pass then correctly failed on formatting, strict typing through PyTorch's
untyped distribution methods, lint, and uncovered validation branches; this is retained in
`000068-phase6-mask-gae-quality.txt`.

The corrected focused gate passed 41 tests. Direct coverage instrumentation reports all 110
statements and 52 branches across both modules covered, with no misses
(`000069-phase6-mask-gae-quality-final.txt`).

## Result

- float32 and BF16 samples are always legal and finite;
- a one-legal-action row is deterministic with zero entropy;
- illegal logits receive exactly zero gradient;
- all-masked rows fail immediately;
- the four-transition hand oracle matches to 1e-12 and includes both perspective signs;
- terminal bootstraps vanish and truncated slices use the signed stored bootstrap;
- batched environment lanes remain independent;
- colour-swapped values/rewards negate outputs;
- all-positive signs match a separate conventional GAE loop bitwise.

## Next

Implement the full-chronology rollout buffer from T8 RED tests, preserving every opponent row for
GAE while exposing trainable-only optimization indices.
