# Iteration 000011 — Mutmut 3.6 Harness Validation

- Work label: Phase 2 mutation-harness validation.
- Starting Git SHA: `5306ef52b17f`.
- Falsifiable objective: the pinned mutation runner must collect the intended rules tests in its
  isolated tree, prove its forced-failure control, and kill a bounded production mutant family
  before the full mutation gate is launched.

## RED and Diagnosis

- The removed Mutmut 2 `--paths-to-mutate` option and a nested single-file `also_copy` target
  failed; current configuration belongs in `[tool.mutmut]`, and nested fixtures must be copied by
  directory. Evidence: `000011-mutmut-config-red.txt`.
- Generated trampolines exposed import-time calls from the geometry builder and generated
  dataclass constructors. Geometry is now a frozen constant whose derivation has an exhaustive
  test; `State`, `Step`, and `MovePath` retain frozen/slots semantics with explicit typed
  constructors. Evidence: `000011-mutmut-probe.txt` through `000011-mutmut-probe-3.txt`.
- A `max_stack_depth = 12` probe was interrupted after roughly 16 CPU-minutes once installed source
  inspection proved that setting caused a path-resolving stack walk on every instrumented call.
  Removing the filter reduced the complete 127-test statistics pass to 134.96 CPU seconds.
  Evidence: `000011-mutmut-probe-4.txt` and `mutants/mutmut-stats.json` (replaceable cache).
- Isolation next exposed missing `docs/RULES.md`; copying `docs/` fixed it. A final wrong mutant-name
  filter was preserved before using Mutmut 3's `x_coord__mutmut_*` naming. Evidence:
  `000011-mutmut-probe-5.txt` through `000011-mutmut-probe-7.txt`.

## GREEN

- Mutmut collected 127 tests, passed clean and forced-failure controls, and killed all 15 generated
  `checkers.rules.board.x_coord__mutmut_*` mutants; exit 0. Evidence:
  `logs/test-output/000011-mutmut-probe-8.txt`.
- Repository gate: Ruff format/lint and strict mypy clean; 148 repository tests and eight property
  tests pass; all six rules modules have 100% statement and branch coverage. Evidence:
  `logs/test-output/000011-check.txt`.

## Result

The mutation harness is reproducible and sensitive. The full 968-mutant rules run remains the
next Phase 2 gate; no score is claimed from this bounded probe.

The first commit attempt was rejected because local Git author configuration was absent. The
existing commit identity was reused repository-locally rather than inventing a new identity;
`logs/test-output/000011-git-identity-red.txt` preserves the failure.
