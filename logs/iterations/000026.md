# Iteration 000026 — Freeze Phase 7 and separate run/trainer state

## Objective

Translate the Phase 7 trainer, league, logging, and resume prose into a falsifiable matrix, then
implement only the immutable/mutable state boundary and historical pool.

## Interpretation

The timed gate is conservatively frozen as at least 1,800 training seconds per seed for three
seeds. A0 current-policy self-play is the smoke baseline; A1–A3 remain Phase 8 Stage-B experiments.
The random evaluation freezes 364 colour-balanced games using `null=.85`, `alternative=.90`,
alpha .05, and target power .80. This does not turn the approximate plan into an exact composite-
hypothesis confidence claim.

## RED / corrections / GREEN

Missing imports made the first suite RED (`000100-*`). The first implementation then exposed a
test-side invalid `None` YAML rendering and a real literal-endpoint schedule defect (`000101-*`).
The strict audit exposed formatting, typing, and validation-coverage gaps (`000103-*`). The final
gate passes 83 tests, strict Ruff/mypy, and all 453 statements/176 branches (`000104-*`).

## Result

- `RunConfig` is frozen and rejects partial/unknown YAML plus invalid domains/cross-field relations;
- LR and entropy schedules are pure and exact at endpoints;
- updates advance only at a complete rollout boundary;
- every global, CUDA, opponent, minibatch, and per-environment RNG state round-trips;
- the initial snapshot is pinned, historical entries FIFO-evict, and all tensors are independent
  CPU clones;
- A0/A1/A2 thresholds and A3 payoff weighting are literal and deterministic.

## Next

Implement the production-engine self-play collector and complete §13.2 metric formulas/alerts under
small hand-derived tests before adding PPO epoch orchestration.
