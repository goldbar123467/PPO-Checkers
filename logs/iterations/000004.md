# Iteration 000004 — R2–R5 Move Transitions

- Work label: Phase 2 correctness baseline.
- Starting Git SHA: `5197a87f07f3`.
- Falsifiable objective: implement mandatory short-step captures, forced continuation, delayed
  removal, promotion, completed-move counters, and exact undo against hand-worked positions.

## RED

- Focused collection failed because `checkers.rules.moves` did not exist; exit 2. Evidence:
  `logs/test-output/000004-red.txt`.
- The first implementation run had 16 passing tests and one test failure. The fixture incorrectly
  asserted that blocking a king's two destinations also immobilized two other friendly men; the
  rule-specific assertion was narrowed to the king without weakening its occupied/flying checks.
- The first full gate passed but showed only 94% on `moves.py`. Public validation branches were
  added and unreachable private defensive branches, already guaranteed by `State`, were removed.
  Evidence: `logs/test-output/000004-check.txt`.

## GREEN

- `legal_steps` enforces capture priority across the player, forced same-piece continuation,
  forward-only men, bidirectional kings, short-range geometry, pending-capture exclusion, and no
  majority-capture selection.
- `apply_step` preserves pending opponent occupancy until sequence completion; promotion ends a
  jump sequence; `ply` counts every environment step while no-progress counters update only at a
  completed move. `Transition.before` provides exact O(1) immutable undo.
- Final increment gate: 78 tests passed and all then-existing rules modules had 100% statement and
  branch coverage. Evidence: `logs/test-output/000004-check-2.txt`.

## Specification Finding

- Exhaustive enumeration of all 32-square short-jump geometries proves a captured midpoint cannot
  be a later landing in the same sequence. BLOCK-002 and ADR-007 record why the required legal-set
  divergence fixture is impossible while exact delayed-removal state remains binding.

## Result

Fast-path R2–R5 transitions are green; Phase 2 remains in progress and has BLOCK-002 attached.
