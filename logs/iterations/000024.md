# Iteration 000024 — Freeze forced-win value targets

## Objective

Complete T7 with three production-engine trajectories of exactly 3, 5, and 7 steps, ensuring the
special terminal multi-jump and R6.2 cases are distinct fixtures.

## Oracle construction

A deterministic random search over valid small-piece boundary states located candidate paths; the
search result itself is not trusted by the test. Each frozen fixture reruns through
`legal_steps`, requires a one-element legal set at every state, and advances through
`CheckersEnv`. Expected ACF steps, actors, move-completion flags, signs, terminal reasons, and
actor-relative targets are literal constants.

With zero values and `gamma=lambda=1`, the hand targets are:

- length 3, R6.2: `[+1,-1,+1]`;
- length 5, terminal four-jump sequence: `[-1,+1,+1,+1,+1]`;
- length 7: `[+1,+1,-1,-1,+1,-1,+1]`.

## Corrections / sensitivity / GREEN

The behavioral test passed immediately because signed GAE already existed. The first two quality
commands exposed only test formatting/lint and an incorrectly scoped standalone mypy invocation;
both transcripts are retained (`000093-*`, `000094-*`). The corrected strict command passes
(`000095-*`). A temporary omission of recursive `sigma` then made all three numerical cases fail
with all-positive targets (`000096-*`). The exact source was restored and all four tests pass again
(`000097-*`).

## Next

Run the complete `make check` Gate 6 and publish the sourced, claim-bounded Phase 6 analysis.
