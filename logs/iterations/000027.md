# Iteration 000027 — Phase 7 collection, update, logging, and exact resume

## Objective

Implement the production A0 self-play path through exact update-boundary persistence before any
timed training claim.

## RED evidence

- `000105-phase7-metrics-selfplay-red.txt`: metric and collector imports absent.
- `000108-phase7-updater-red.txt`: training orchestrator absent.
- `000110-phase7-checkpoint-red.txt`: checkpoint module absent.
- `000112-phase7-resume-red.txt`: session-level resume orchestrator absent.

## Findings and corrections

- The hand fixture initially had two legal opening captures despite asserting one; removed the
  unintended second Red man instead of teaching the collector a false legal count.
- Weighted two-bin calibration error is 0.5, not the initially transcribed 0.3.
- The literal §13.2 inventory has 55 keys: optimization has 12 including `charts/SPS`.
- Normalized entropy is undefined when every state has `k=1`; the alert now ignores an all-forced
  batch rather than treating the declared zero diagnostic as policy collapse.
- Integer object IDs can be reused after garbage collection; exactly-once rollout tracking now uses
  weak live-object identity rather than an unbounded set of integer IDs.

## GREEN evidence

- Self-play/metrics: 17 focused tests pass (`000107`).
- W&B fake-run and credential contracts: 4 focused tests pass (`000109`).
- Checkpoint round trip and hostile-pickle rejection: 4 focused tests pass.
- CPU R2: checkpoint after jump one; updates 2–11 are bitwise identical (`000113`).
- Intermediate repository integration: 872 tests pass, total coverage 95.26% (`000111`).

## Claim boundary

This proves deterministic engineering behavior on the current CPU stack. It does not yet prove the
same-stack CUDA resume tolerance, a real W&B offline directory, 30 minutes of training, or playing
strength. Those remain explicit Phase 7 work.
