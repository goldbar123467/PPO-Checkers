# Iteration 000003 — Board Geometry and Immutable State

- Work label: Phase 2 correctness baseline.
- Starting Git SHA: `a7a88ba`.
- Falsifiable objective: encode the frozen ACF board and every §5.1 state invariant with typed,
  immutable primitives; achieve the configured coverage threshold without weakening it.

## RED

- Focused board/state collection failed because `checkers.rules.board` and
  `checkers.rules.state` did not exist; exit 2. Evidence: `logs/test-output/000003-red.txt`.
- The first full gate found lint/type defects in the new files; exit 2. Evidence:
  `logs/test-output/000003-check.txt`.
- The second full gate passed all 45 tests but failed the 92% coverage threshold at 88.37%; exit 2.
  Evidence: `logs/test-output/000003-check-2.txt`.
- After adding focused negative tests, the normal suite reached 100%, but the fuzz-only subprocess
  started a fresh global coverage run and failed at 0.58%; exit 2. Evidence:
  `logs/test-output/000003-check-3.txt`.

## GREEN

- Corrected the previously mirrored ACF diagram against the primary source before implementing
  geometry; ADR-006 records the near-miss and frozen mapping.
- Added exact ACF coordinate/rotation/double-corner helpers and a frozen, slot-based `State` with
  explicit `PlayerId`, capture-sequence fields, uint32 checks, disjointness, and counter checks.
- Added negative tests for every runtime validation branch. No coverage threshold was lowered.
- Made fuzz/perft subprocesses explicitly coverage-neutral; `make test` remains the whole-project
  coverage authority and `make check` still requires both the full suite and fuzz target.
- Final `make check`: exit 0; 57 passed plus 6 fuzz/scaffold tests; board/state statement and branch
  coverage 100%. Evidence: `logs/test-output/000003-check-4.txt`.

## Result

R1 and §5.1 primitives are green. Gate 2 remains open pending R2–R5/R7, oracle differential,
published transcripts, mutation, and large-run evidence.
