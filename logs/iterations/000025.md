# Iteration 000025 — Complete the offline RL-core gate

## Objective

Replay the entire repository after all focused Phase 6 oracles are green, measure the actual CUDA
fixture envelope, and publish a primary-source-bounded conclusion before advancing state.

## Consolidated result

At source `165c72101c71a77317b1848a316b4b4a22c46654`, `make check` passed:

- 88 paths formatted, Ruff-clean, and strict-mypy clean;
- 760/760 tests with no skips/xfailed;
- 3,842/3,842 statements and 1,358/1,358 branches covered;
- eight additional property tests;
- real D2 CPU and D3 RTX determinism paths.

The gate ran from 03:28:11 to 03:30:23 EDT. Its SHA-256 is
`3a0abe5ea6d27b6e85f736ec959be5aa069ad093a29687dda6ed481afb824e14`.

## Hardware follow-up

The first two peak-memory probes called PyTorch's reset API before CUDA context initialization and
failed with `Invalid device argument`; both are preserved as `000098-*` and `000099-*`. After
explicit context initialization, one ten-update RTX fixture measured 73.125 MiB peak allocated and
94.000 MiB peak reserved, against 12,227 MiB total device memory.

## Claim boundary

Gate 6 verifies offline mechanics and capacity only. It is not evidence that PPO learns checkers,
converges, generalizes, or beats any fixed opponent. `reports/phase6_rl_core_analysis.md` records
the original PPO/GAE papers, masking paper, official PyTorch APIs, exact oracles, negative controls,
hardware stack, hashes, corrections, and remaining uncertainty.

## Next

Freeze the Phase 7 self-play, league, metric, W&B, and checkpoint/resume contracts before adding
trainer code or launching a timed smoke.
